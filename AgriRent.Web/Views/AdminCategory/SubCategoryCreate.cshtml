@model AgriRent.DTOs.SubCategoryDto
@{
    ViewData["Title"] = "Create Subcategory";
    Layout = "_AdminLayout";
}

<style>
    /* Premium styles for Create/Edit forms */
    :root {
        --primary-accent: #0f766e; /* Teal theme */
        --primary-accent-hover: #0d9488;
        --card-bg: rgba(255, 255, 255, 0.9);
        --input-bg: #f8fafc;
        --input-border: #e2e8f0;
    }

    .form-container-premium {
        max-width: 800px;
        margin: 0 auto;
    }

    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: #64748b;
        text-decoration: none;
        font-weight: 500;
        margin-bottom: 20px;
        transition: color 0.2s;
    }

    .back-link:hover {
        color: var(--primary-accent);
    }

    .premium-card {
        background: var(--card-bg);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.6);
        border-radius: 20px;
        padding: 40px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.04);
    }

    .premium-header {
        display: flex;
        align-items: center;
        gap: 15px;
        margin-bottom: 30px;
        padding-bottom: 20px;
        border-bottom: 1px solid #f1f5f9;
        position: relative;
    }

    .category-tag {
        position: absolute;
        right: 0;
        top: 0;
        font-size: 0.8rem;
        background: #f0fdfa;
        color: #115e59;
        padding: 6px 15px;
        border-radius: 20px;
        font-weight: 600;
        border: 1px solid #ccfbf1;
    }

    .premium-header-icon {
        width: 50px;
        height: 50px;
        background: linear-gradient(135deg, #14b8a6, #0f766e);
        color: white;
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        box-shadow: 0 4px 15px rgba(15, 118, 110, 0.3);
    }

    .premium-header h3 {
        margin: 0;
        font-weight: 700;
        color: #1e293b;
    }

    .premium-input-group {
        margin-bottom: 25px;
    }

    .premium-label {
        display: block;
        font-weight: 600;
        color: #475569;
        margin-bottom: 8px;
        font-size: 0.95rem;
    }

    .premium-control {
        width: 100%;
        padding: 14px 20px;
        background-color: var(--input-bg);
        border: 2px solid var(--input-border);
        border-radius: 12px;
        font-size: 1rem;
        color: #334155;
        transition: all 0.3s ease;
    }

    .premium-control:focus {
        outline: none;
        border-color: var(--primary-accent);
        background-color: #fff;
        box-shadow: 0 0 0 4px rgba(15, 118, 110, 0.1);
    }

    .btn-submit-premium {
        background: linear-gradient(135deg, #14b8a6 0%, #0f766e 100%);
        color: white;
        border: none;
        padding: 16px 30px;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1.1rem;
        width: 100%;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 8px 20px rgba(15, 118, 110, 0.25);
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        margin-top: 10px;
    }

    .btn-submit-premium:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 25px rgba(15, 118, 110, 0.35);
    }
</style>

<div class="form-container-premium">
    <a href="/AdminCategory/SubCategories/@Model.CategoryId" class="back-link">
        <i class="fa-solid fa-arrow-left-long"></i> Back to Subdirectories
    </a>

    <div class="premium-card">
        <div class="premium-header">
            <div class="premium-header-icon">
                <i class="fa-solid fa-plus"></i>
            </div>
            <div>
                <h3>Create New Subcategory</h3>
                <span style="color: #64748b; font-size: 0.9rem;">Add a new component under the main category</span>
            </div>
            @if (ViewBag.CategoryName != null)
            {
                <div class="category-tag">Parent: @ViewBag.CategoryName</div>
            }
        </div>
        
        <form action="/AdminCategory/SubCategoryCreate" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" name="CategoryId" value="@Model.CategoryId" />
            
            <div class="premium-input-group">
                <label class="premium-label">Subcategory Name</label>
                <input type="text" name="SubCategoryName" class="premium-control" placeholder="e.g. John Deere, Accessories" required />
                <span class="text-danger" style="font-size: 0.85rem; margin-top: 5px; display: block;">@Html.ValidationMessage("SubCategoryName")</span>
            </div>

            <div class="premium-input-group">
                <label class="premium-label">Visibility Status</label>
                <select name="Status" class="premium-control">
                    <option value="Active" selected>Active</option>
                    <option value="Inactive">Inactive</option>
                </select>
                <div style="font-size: 0.85rem; color: #64748b; margin-top: 8px;">
                    <i class="fa-solid fa-circle-info me-1"></i> Marking this inactive prevents new products from using it.
                </div>
            </div>

            <button type="submit" class="btn-submit-premium">
                <i class="fa-solid fa-check"></i> Create Subcategory
            </button>
        </form>
    </div>
</div>
